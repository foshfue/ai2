import type { VFC } from "react";

const Head: VFC = () => {
    return (

        <meta
            key="viewport"
            name="viewport"
            content="width=device-width, initial-scale=1"
        />

    );
};

export default Head;
